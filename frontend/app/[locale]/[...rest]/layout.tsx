import { ChildrenProp } from '@/app/lib/definitions';
import FullScreenLayout from '@/app/ui/layouts/DefaultLayout';

const RestLayout = ({ children }: ChildrenProp) => <FullScreenLayout>{children}</FullScreenLayout>;

export default RestLayout;
