import { Metadata } from 'next';

// ------------------ begin :: metadata ------------------
export const metadata: Metadata = Object.freeze({
  title: 'Licenses',
} as const);
// ------------------ end :: metadata ------------------

const LicensesPage = () => {};

export default LicensesPage;
